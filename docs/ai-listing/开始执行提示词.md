你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发，不要凭经验直接改代码。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档

本次只执行 Phase 0：项目启动与环境确认。

要求：
1. 先判断需要使用哪些 NocoBase 官方 skills，并读取对应 SKILL.md。
2. 优先使用官方 skills，例如 nocobase-env-manage、nocobase-plugin-manage、nocobase-plugin-development。
3. 如果当前环境缺少某个官方 skill，请先说明缺失项和 fallback，不要直接凭经验修改。
4. 检查当前目录是否是 NocoBase 项目根目录。
5. 读取 package.json、插件目录、NocoBase 版本、运行方式、已有插件状态。
6. 不要实现业务功能，不要创建大量页面，不要做真实抓取、AI、发布逻辑。
7. 如果需要创建插件，只输出方案，不要直接 scaffold，等我确认。
8. 输出 Phase 0 交付说明，必须包含：
  - 已读取的官方 skills
  - 当前项目结构判断
  - NocoBase 版本和运行方式
  - 插件开发路径建议
  - 是否建议创建 @crossborder/plugin-ai-listing
  - 风险和下一步 Phase 1 建议


继续执行 Phase 1：数据模型与权限骨架。

请先阅读 phase 文档中的 Phase 1，并优先使用 NocoBase 官方 skills：
- nocobase-data-modeling
- nocobase-acl-manage
- 必要时使用 nocobase-plugin-development

本次只做数据模型和权限骨架，不做页面、不做抓取、不做发布。

要求：
1. 先读取现有 collections、fields、roles、permissions，避免重复建模。
2. 设计并实现商品、SKU、媒体、抓取任务、任务步骤、规则、审计、发布记录等核心 collections。
3. 所有任务相关表必须预留 traceId、errorCode、errorMessage、retryable、status、metadata。
4. 权限要区分管理员、运营、审核人员、AI 员工工具权限。
5. 如果某些表更适合由插件 collection 定义，请说明原因。
6. 完成后提供：
  - 已读取和使用的官方 skills
  - 修改文件清单
  - 数据模型清单
  - 权限设计
  - 读回验证结果
  - 下一步 Phase 2 建议



你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发，不要凭经验直接改代码。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档
2. 
本次只执行 Phase 2：应用外壳与视觉基础。

必须优先使用 NocoBase 官方 skills：
- nocobase-ui-builder
- nocobase-plugin-development
- 必要时使用 nocobase-prototype-repro / nocobase-utils

要求：
1. 先判断并读取对应官方 SKILL.md，再执行。
2. 只完成页面路由、导航、页面壳、统一卡片/表格/按钮/状态标签样式和空态。
3. 页面包括：工作台、商品抓取、信息处理、预览编辑、商品发布、商品库、规则管理、发布记录、设置。
4. 所有客户端代码必须在 src/client-v2。
5. 不使用 this.app.use()，不新增全局 React Provider。
6. 建立插件范围内的 ListingPageErrorBoundary，防止页面白屏。
7. 建立 requestWithFriendlyError 或同等请求封装，错误提示必须包含 friendlyMessage、errorCode、traceId、下一步动作。
8. 不实现真实抓取、AI、处理、发布逻辑。
9. 不修改数据库连接配置，不切回 SQLite，继续使用 PostgreSQL 目标配置。

完成后输出：
- 已读取的官方 skills
- 实际使用的官方 skills
- 未使用但相关的 skill 及原因
- 修改文件清单
- 每个页面路径
- 错误边界和 API 错误封装说明
- 手动模拟页面异常/API 失败的验证结果
- 样式截图建议



你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档

本次只执行 Phase 3：工作台 Dashboard。

必须优先使用 NocoBase 官方 skills：
- nocobase-ui-builder
- nocobase-data-analysis
- 必要时使用 nocobase-plugin-development
- 还有其他的 skill

要求：
1. 先判断并读取对应官方 SKILL.md，再执行。
2. 实现工作台页面：KPI 卡片、快速入口、平台连接状态、最近任务、通知入口。
3. 实现 aiListingDashboard:summary API，占位返回稳定结构。
4. 可以使用 mock 数据，但 API 结构必须稳定，后续能替换为真实聚合。
5. 快速入口要能跳转到商品抓取页对应 tab。
6. 最近任务点击可进入对应页面或展示占位详情。
7. 平台连接状态支持 connected / expired / disconnected。
8. 不开发抓取执行、信息处理、发布逻辑。
9. 继续复用 Phase 2 的 ListingPageErrorBoundary 和 requestWithFriendlyError。

完成后输出：
- 已读取和使用的官方 skills
- 修改文件清单
- 页面路径
- summary API 返回示例
- 正常路径自测结果
- API 失败友好错误提示验证
- 样式截图建议



你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档

本次只执行 Phase 4：URL 抓取 MVP。

必须优先使用 NocoBase 官方 skills：
- nocobase-plugin-development
- nocobase-data-modeling
- nocobase-workflow-manage
- 必要时使用 nocobase-ui-builder

要求：
1. 先判断并读取对应官方 SKILL.md，再执行。
2. 实现商品抓取页 URL 抓取 tab。
3. 实现 aiListingCapture:startUrlCapture API。
4. 实现 aiListingTasks:getProgress API。
5. 创建抓取任务、任务步骤，并成功生成商品草稿。
6. Alibaba.com 采集优先按 OpenAPI adapter 设计，可先 mock 返回。
7. Alibaba.com OpenAPI 本地参考目录：
   /Users/wuzhixuan/code/project/nocobase/packages/plugins/@nocobase/plugin-ai-listing-workbench/docs/openapi
8. Crawl4AI 只作为兜底 adapter 接口预留，不要把浏览器运行在 NocoBase 主进程。
9. 抓取 API 必须返回标准错误结构。
10. 任务步骤必须写入 traceId、errorCode、retryable、错误摘要。
11. 前端失败时必须显示友好错误、重试入口和 traceId。
12. 不实现店铺抓取、关键词抓取、批量导入、信息处理和发布。

完成后输出：
- 已读取和使用的官方 skills
- 修改文件清单
- 测试 URL
- API 请求/响应示例
- 任务记录和任务步骤读回结果
- 生成的商品草稿读回结果
- 失败示例、错误码、traceId
- 页面截图建议

你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档

本次只执行 Phase 5：商品抓取扩展。

必须优先使用 NocoBase 官方 skills：
- nocobase-ui-builder
- nocobase-plugin-development
- nocobase-data-modeling
- 必要时使用 nocobase-workflow-manage

要求：
1. 先判断并读取对应官方 SKILL.md，再执行。
2. 补齐商品抓取页三个 tab：店铺抓取、关键词抓取、批量导入。
3. 店铺抓取支持店铺 URL、分析店铺、商品列表、筛选、选中抓取。
4. 关键词抓取支持关键词、平台、排序、价格区间、结果卡片、选中抓取。
5. 批量导入支持 CSV/Excel 上传、文本粘贴、模板下载、导入历史。
6. OpenAPI 优先，Crawl4AI 只做公开页面兜底接口。
7. 批量导入必须显式创建任务明细，不依赖 collection event 批量触发。
8. 每条 URL 都写 aiListingTaskSteps。
9. 批量任务必须展示成功/失败统计，单条失败不能影响其他条。
10. 错误行要有可读提示、错误码、traceId、可重试状态。
11. 不开发信息处理、预览编辑、发布逻辑。

完成后输出：
- 已读取和使用的官方 skills
- 修改文件清单
- 三个 tab 页面路径
- 店铺抓取样例
- 关键词抓取样例
- 批量导入样例文件/文本
- 批量任务明细读回结果
- 成功/失败统计验证
- 失败行错误提示和 traceId 验证
- 样式截图建议


你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发，不要凭经验直接改代码。


请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档
2. 以及记忆文件


本次只做当前 Phase，不要提前开发后续 Phase。
必须先判断并读取对应 NocoBase 官方 SKILL.md。
完成后必须输出：已读取/使用的官方 skills、修改文件、页面路径/API/Workflow、自测结果、异常路径、traceId/错误码/友好错误提示、样式验收建议。
执行 Phase 6：信息处理与规则管理 MVP。

优先使用官方 skills：
- nocobase-workflow-manage
- nocobase-ai-employee
- nocobase-ui-builder
- 必要时使用 nocobase-data-modeling / nocobase-plugin-development

要求：
1. 实现规则选择、待处理商品、批量处理、处理进度、基础规则管理和审计日志。
2. Workflow 编排处理流程，商品状态进入 processing / processed / process_failed。
3. AI 员工只能写建议字段，不能直接覆盖最终字段；写最终字段必须 Ask。
4. 规则执行和字段变化必须写审计日志。
5. 媒体处理先做任务结构和状态，不要求真实去水印/白底图。
6. 失败态必须有错误码、traceId、可重试提示和修复入口。
7. 不开发预览编辑和发布。

完成后提供 workflow 执行记录、AI 建议字段样例、审计日志样例、失败路径样例、页面截图说明。


--- 

你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发，不要凭经验直接改代码。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档
2. 以及记忆文件


本次只做当前 Phase，不要提前开发后续 Phase。
必须先判断并读取对应 NocoBase 官方 SKILL.md。
完成后必须输出：已读取/使用的官方 skills、修改文件、页面路径/API/Workflow、自测结果、异常路径、traceId/错误码/友好错误提示、样式验收建议。
执行 Phase 7：预览编辑与人工审核。

优先使用官方 skills：
- nocobase-ui-builder
- nocobase-ai-employee
- 还有其他官网 skill
- 必要时使用 nocobase-plugin-development / nocobase-prototype-repro

要求：
1. 重点还原设计稿交互和视觉层级。
2. 完成左侧商品列表、右侧商品详情、图片预览、字段编辑、变更记录、审核操作和 AI 快捷按钮。
3. 支持搜索、状态筛选、平台筛选、批量选择。
4. 字段编辑包括标题、价格、库存、SKU、描述、参数。
5. 审核通过后状态进入 reviewed，关键字段默认锁定。
6. 审核后再次编辑需要回退或明确提示，并写审计日志。
7. AI 按钮只做建议或 Ask 后填表，不能直接发布。
8. 不开发真实发布。

完成后提供多个商品样例、编辑记录、审核状态变化、AI 按钮测试、页面截图说明。

--- 

你是本项目的 NocoBase AI 开发者。请严格按文档和 NocoBase 官方 skills 开发，不要凭经验直接改代码。

请先阅读：
1. /Users/wuzhixuan/code/project/nocobase/docs/ai-listing 下面的文档
2. 以及记忆文件


本次只做当前 Phase，不要提前开发后续 Phase。
必须先判断并读取对应 NocoBase 官方 SKILL.md。
完成后必须输出：已读取/使用的官方 skills、修改文件、页面路径/API/Workflow、自测结果、异常路径、traceId/错误码/友好错误提示、样式验收建议。

执行 Phase 8：发布前校验与模拟发布。

优先使用官方 skills：
- nocobase-workflow-manage
- nocobase-plugin-development
- nocobase-ui-builder

要求：
1. 实现商品发布页面、发布配置、待发布商品、发布前校验、模拟发布、发布进度和发布记录。
2. 实现 aiListingPublish:precheck 和 aiListingPublish:publish。
3. Workflow 编排发布前校验和模拟发布。
4. 未设置类目、图片缺失、标题/价格/SKU 异常时必须校验失败。
5. 校验通过后才能模拟发布。
6. 真实平台发布只保留 adapter 接口，不启用真实发布。
7. 发布动作必须幂等，重复点击不能重复创建发布记录。
8. 失败原因必须可读，并展示错误码、traceId 和下一步动作。

完成后提供校验失败样例、校验成功样例、模拟发布记录、幂等验证、页面截图说明。

---
执行 Phase 9：商品库、发布记录、设置。

优先使用官方 skills：
- nocobase-ui-builder
- nocobase-data-analysis
- 必要时使用 nocobase-plugin-development

要求：
1. 使用 UI Builder 优先实现商品库、发布记录和设置页。
2. 商品库支持统计卡、搜索筛选、列表/卡片视图、状态、发布链接。
3. 发布记录支持统计卡、筛选、目标链接、失败原因、重试、导出占位或 MVP。
4. 设置页展示平台账号状态、默认平台、默认规则、Crawl4AI 开关、OpenAPI 状态。
5. 平台账号只展示授权状态，不展示密钥。
6. OpenAPI IP 白名单和 token 过期状态需要友好提示。
7. 复杂导出/链接聚合可用插件 API。
8. 失败项可进入重试流程或占位。

完成后提供页面路径、筛选测试、状态展示、设置页截图、导出/重试占位说明。
---
